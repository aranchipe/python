#grades = [35, 67, 98, 100, 100]
#total = 0
#amount = len(grades) #length

#for grade in grades:
#	total = total + grade

#media = total/amount

#print(media)

#_______________________________________________________

grades = [35, 67, 98, 100, 100]
total = sum(grades) #soma dos elementos do array
amount = len(grades) #length

media = total/amount

print(media)